# looklift 产品需求文档

> 本文档是产品的"宪法":定位、用户、最终形态、路线图。每轮迭代前先对照/更新本文档。
> 敲定于 2026-07-16(v0.2 发布后),与作者 brainstorm 确认。
> 文档约定:每个迭代一个 spec 文件夹 `docs/specs/v0.X/`,固定三份文档(需求/设计/任务),
> 规范见 [specs/README.md](specs/README.md);[design.md](design.md) 只记录已实现的架构实况。

## 一句话定位

**looklift 是给摄影爱好者的开源 AI 调色逆向工具**——看到喜欢的照片,扔进来,
它告诉你这是怎么调的,并给你一个能直接用的 Lightroom 预设。

把「调色审美」变成「可复用、可学习、可分享的资产」。

**对外定位语**(竞品分析结论,见 [competitors.md](competitors.md);措辞 2026-07-16 修正,
确保与事实一致——云端模型分析时照片会发给该服务商):
> LUT 工具给你黑盒,looklift 给你白盒——参数可改、原理可学、预设可攒。
> 预设生成、渲染、风格库全部在你电脑上;AI 分析只把照片发给你自己选的模型,
> 接本地模型(Ollama)可彻底离线。

市场空白:参考图取色流派(Color.io 等)只出黑盒 LUT,风格学习流派(Imagen/Aftershoot/Neurapix)
只卖"省时间"——**没有人做"教学讲解"**,「懂」是我们独占的位置。

## 目标用户(按优先级)

| 画像 | 描述 | 需要什么 |
|---|---|---|
| **P1 入门爱好者**(最大人群,作者本人) | 会用 LR 但不懂参数原理,看到好片不知道怎么学 | GUI、拖拽、中文讲解、一键出预设——工具即老师 |
| **P2 进阶玩家** | 有明确风格追求,在乎还原度 | 批量、refine 精修闭环、风格库管理 |
| **P3 技术型用户** | 会命令行,要自动化 | CLI(已有)、可脚本化、CI 可跑 |

## 核心价值三条线(都做,按节奏推进)

1. **像(还原度)**——套预设后与目标成片的相似度,从六七分做到九分。
   技术抓手:本地近似渲染预览、refine 自动闭环、还原度评分。
2. **库(风格资产)**——风格收藏、检索、标签、批量分析、聚类、分享。
3. **懂(调色教学)**——报告即调色课:参数为什么这么调、经典风格拆解、预置 look 库。

## 最终形态(v1.0)

**功能齐全的本地 GUI 应用**:
- 拖入照片 → 实时看分析过程 → 风格报告(讲解+参数+曲线) → 一键导出预设/sidecar
- 风格库面板:收藏、搜索、套用、分享
- 形态(2026-07-16 敲定):pywebview 独立窗口(像正经桌面软件),`--browser` 浏览器模式兜底
- 打包成 Windows exe(不会装 Python 的人双击即用),CLI 与 GUI 共享同一核心
- 开源(MIT),面向摄影社区发布

## AI 访问策略(关键产品决策)

非技术摄影用户的最大门槛是 AI 额度。方案:**多供应商体系,逐步支持**:

| Provider | 说明 | 排期 |
|---|---|---|
| 本地 Claude Code CLI | 走 CC 订阅额度,零配置(已装即用) | ✅ v0.1 |
| Anthropic 官方 API | BYOK,结构化输出最稳 | ✅ v0.1 |
| OpenAI 兼容中转/供应商 | 国内用户主流方式;自定义 base_url + key + 模型名 | 🎯 计划中 |
| 本地部署视觉模型 | Ollama / vLLM(Qwen-VL 等),免额度的终极方案 | 🎯 计划中 |

设计要求:provider 抽象层,新增供应商不动业务逻辑;GUI 首次启动引导配置。

## 功能模块总览(v1.0 全景)

三层九模块;核心原则:引擎层是唯一的功能实现,CLI 与 GUI 都只是入口。

**核心引擎层**
| # | 模块 | 功能 | 落点 |
|---|---|---|---|
| 1 | AI 分析引擎 | 单图/原片对照/多图归纳 → 参数+中文讲解;多供应商(CC/API 已有,中转/Ollama v0.5) | analyzer, providers |
| 2 | 参数读写 | 读 JPEG 内嵌 LR 元数据(100% 精确);写预设 .xmp / RAW sidecar | xmp_reader/writer ✅ |
| 3 | 渲染与评分 | preview 本地近似渲染;还原度 0-100;.cube LUT 导出 | render, v0.3 |
| 4 | 精修闭环 | refine 手动(✅)+ auto 自动迭代到收敛(v0.3) | refine |
| 5 | 风格库 | 收藏/套用/批量(✅);强度滑杆(v0.4);预置 10 经典 look(v0.6);目录聚类(v0.5+) | looks/ |
| 6 | 报告与教学 | HTML 报告(✅);"调色课"式讲解+look 拆解课(v0.6)——「懂」独占赛道 | report |

**界面层**:7. CLI(✅,全功能可脚本化)| 8. GUI(v0.4,pywebview,Claude 风 tokens,
拖拽→分析→报告→导出、库面板、对比条、首次配置向导)

**分发层**:9. pip(✅)+ Windows 单 exe(v0.7);Mac 走 pip

主流程:照片 → AI 逆向白盒参数 → 本地渲染验证 → 自动精修 → 入风格库 →
出预设/LUT/报告课。价值线归属:「像」=3+4,「库」=5,「懂」=6。

## 用户故事总表

| # | 用户故事 | 状态 |
|---|---|---|
| U1 | 给一张大师成片,想知道调色参数和后期思路讲解 | ✅ v0.1 |
| U2 | 原片+成片对照,分析更精确 | ✅ v0.1 |
| U3 | 自己导出的 JPEG 带 LR 元数据,直接提取精确参数 | ✅ v0.1 |
| U4 | 分析结果做成模版,收藏成风格库随时套用 | ✅ v0.1 |
| U5 | 模版一键套到 RAW(批量) | ✅ v0.1 |
| U6 | 套用后不够像,AI 对比差异迭代修正 | ✅ v0.1 refine |
| U7 | 从多张作品归纳一位摄影师的风格 | ✅ v0.2 |
| U8 | 好看的风格报告,方便保存/分享 | ✅ v0.2 |
| U9 | 直接敲 `looklift` 命令 | ✅ v0.2 |
| U10 | 不开 LR 就能预览参数套用后的效果 | 🎯 v0.3 |
| U11 | 让 AI 自动迭代到"够像"为止,不用我来回跑 | 🎯 v0.3 |
| U12 | 不会命令行,想要拖拽图片的界面 | 🎯 v0.4 |
| U13 | 用国内 API 中转站或本地模型,不依赖 Anthropic | 🎯 v0.5 |
| U14 | 整个目录批量分析,自动归纳出几种风格 | 🎯 v0.5+ |
| U15 | 报告讲得更深:每个参数为什么这么调 | 🎯 v0.6 |
| U16 | 预置经典风格库(胶片/电影感/日系...)开箱即用 | 🎯 v0.6 |
| U17 | 双击 exe 就能用,不装 Python | 🎯 v0.7 |
| U18 | 手机修图党想要 LR Mobile 可用的 DNG 预设 | 📋 backlog |
| U19 | 把风格导出成 .cube LUT,在达芬奇/剪映里给视频调色用 | 🎯 v0.3 |
| U20 | 预设想只套 70% 的强度 | 🎯 v0.4(GUI 强度滑杆) |
| U21 | 在 LR 里直接右键提取/套用风格(插件) | 📋 backlog |
| U22 | 分享/下载别人的风格(社区) | 📋 backlog(远期) |
| U23 | 丢一张没修过的原片,AI 从我的风格库推荐合适的 look(带预览),或直接给一套合适的后期参数;满意就入库 | 🎯 v0.6 候选(2026-07-16 提出) |

## 迭代路线图

> 排序逻辑(与作者确认):先把还原度做硬(v0.3)→ 尽早上 GUI 壳收集反馈(v0.4)→
> 之后功能都长在 GUI 里慢慢改。

| 版本 | 主题 | 核心交付 | 验收 |
|---|---|---|---|
| v0.3 | **像:精度闭环** | `preview` 本地近似渲染;`refine --auto` 自动迭代至收敛;还原度评分;`.cube` LUT 导出 | 对同一目标图,auto-refine 3 轮后相似度可量化提升;LUT 在达芬奇可加载 |
| v0.4 | **GUI alpha** | 本地界面:拖拽→分析→报告→导出预设;风格库列表;强度滑杆、before/after 对比条;复用全部核心逻辑 | 不碰命令行完成 U1/U4/U8 全流程 |
| v0.5 | **供应商+库** | OpenAI 兼容 provider、Ollama provider;GUI 内配置;批量分析 | 用非 Anthropic 供应商跑通 analyze |
| v0.6 | **懂:教学深化** | 报告升级"调色课";预置 10 个经典 look;(视情况)风格聚类 | 入门用户看完报告能说出"为什么" |
| v0.7 | **分发** | PyInstaller 打包 exe;首次启动引导;使用文档/视频 | 无 Python 环境的机器双击可用 |
| v1.0 | **发布** | 打磨、修 bug、摄影社区发布(少数派/小红书/即刻) | 有真实外部用户反馈 |

## 非目标

- 不做云服务/账号体系(保持本地工具,零运营成本)
- v1.0 打包只做 Windows exe;**macOS 保持核心兼容**(纯 Python 核心 + pywebview 天然跨平台,
  CI 矩阵含 macOS 保证不退化),Mac 用户走 `pip install looklift`;
  .app 打包与苹果签名($99/年,与零成本定位冲突)v1.0 后视社区需求再议
- 不做局部调整(蒙版/渐变)的视觉逆向——不可靠,以文字讲解代替
- 不追求与 LR 渲染管线像素级一致——preview 是"方向正确的近似"
- v1.0 前不做 Capture One / DxO 等其他软件格式
- v1.0 前不做多语言/i18n:GUI、报告、讲解、文档全中文(目标用户与发布渠道都在中文圈),
  README 附英文简介段;英文化 v1.0 后视社区需求
