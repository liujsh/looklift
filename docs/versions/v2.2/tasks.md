# v2.2 任务：平台外壳

> 状态：已确认（2026-07-20，作者逐段确认）。
> 同迭代：[需求](./requirements.md) · [设计](./design.md)。
> 三文档书面复核后再生成 `plans/implementation.md`，按线性单智能体方式执行，不派子代理。

## T1 版本规格与状态同步

- [x] 建立 v2.2 需求、设计和任务三文档，锁定平台外壳与 v2.3 边界。
- [x] 更新文档索引和产品路线图中的 v2.1 验收、v2.2 规格状态。
- [x] 进行一次占位、矛盾、范围和本地链接检查后停止文档扩写。
- 验收：三文档契约一致，没有提前实现图库或设备导入的文字承诺。

## T2 最近正式会话

- [x] 在现有 SessionStore 上增加有限、按更新时间排序的最近会话只读查询。
- [x] 返回正式版本摘要和源文件可用性，不返回临时预览或完整图库实体。
- [x] 接通 HTTP、前端 client 与类型，覆盖 limit、排序、缺失文件和错误测试。
- 验收：首页可以可靠区分可恢复与源文件不可用的正式会话，无数据库迁移。

## T3 平台标签模型与 Studio 运行时

- [x] 建立首页、平台说明页和 Studio 三类标签及活动标签状态。
- [x] 为每个 Studio 创建独立 Store、会话协调器、聊天工作流和清理生命周期。
- [x] 覆盖首页固定、重复 session 聚焦、多运行时隔离和关闭后拒绝异步结果。
- 验收：两个 Studio 的参数、聊天、请求和候选互不影响，同一 session 不重复打开。

## T4 现有 Studio 依赖注入

- [ ] 让 EditorShell 和编辑子流程消费所属运行时，不再隐式绑定唯一全局 Store。
- [ ] 为活动状态建立明确入口，非活动 Canvas 不响应窗口级拖放。
- [ ] 运行受影响的 Store、Canvas、Chat、Session 和 EditorShell 测试及必要 TypeScript 检查。
- 验收：标签切换保留现有 Studio 状态，v2.1 AI 锁、预览竞态和直方图行为不回归。

## T5 快速修图文件选择

- [ ] 接入官方 Tauri 文件对话框及最小 capability，取得稳定原文件路径。
- [ ] 保留普通浏览器的现有上传回退，并处理取消、格式错误和读取失败。
- [ ] 对已存在的 session 聚焦或恢复，对新路径建立正式会话后再打开标签。
- 验收：取消不产生状态，成功只打开一个对应 Studio，失败不留下空白标签。

## T6 首页、导航与标签界面

- [ ] 按已确认原型实现顶部标签、可折叠导航和首页真实内容。
- [ ] 标签栏 `＋` 复用首页三种开始方式，不重复实现动作编排。
- [ ] 让“快速修图”“继续修图”可用；“添加文件夹”“从设备导入”明确标注 v2.3。
- [ ] 为图库、模板、自动化、插件、设置提供统一版本说明，不放假数据。
- 验收：用户可以从首页完成开始、恢复和顶层导航，并清楚识别尚未开放能力。

## T7 关闭门禁与资源释放

- [ ] 实现 AI 请求运行中、存在临时预览和纯正式状态三类关闭路径。
- [ ] “保留并关闭”等待事务成功，“放弃并关闭”只丢候选，“取消”保持原状。
- [ ] 关闭后释放监听器、请求和预览资源，并拒绝晚到响应。
- 验收：任何关闭路径都不会静默保存、静默丢失或污染其他标签。

## T8 视觉与桌面响应式

- [ ] 复用原型的信息层级和 LookLift 现有视觉语言，完成平台页与 Studio 外层布局。
- [ ] 用 `pnpm dev` 人工检查约 1440px、1100px 和 900px，调整纯视觉问题。
- [ ] 不为间距、颜色、圆角、字号或阴影编写像素断言。
- 验收：导航和标签不挤坏画布，最小窗口仍保留可用工作台或明确提示。

## T9 集成、文档回填与版本收口

- [ ] 使用假 AI 和临时文件完成首页→快速修图→多标签→临时预览→关闭→重启恢复流程测试。
- [ ] 在真实 Windows/Tauri 验证原生文件选择、后台 AI 归属、源文件缺失和关闭提示。
- [ ] 回填 `docs/product/architecture.md`、`docs/history/dev-log.md` 和版本状态，统一版本号 2.2.0。
- [ ] 仅在收口或提交前运行一次 Python 与前端全量验证。
- 验收：自动化和人工验收通过，架构实况与代码一致，未开始 v2.3。

## 验证约束

- 迭代中 Python 只运行受影响测试与必要 Ruff 目标。
- 迭代中前端只运行受影响的 Vitest 文件和必要的 `pnpm exec tsc --noEmit`。
- 纯样式改动使用 `pnpm dev` 人工查看，不运行 Vitest 或全量构建。
- 中途只有依赖、import、静态资源或 Vite 配置变化时才按需验证生产构建。
- 版本收口只运行一次：`pytest -q`、Ruff、`pnpm test && pnpm build`；不再额外重复 TypeScript 检查。
- 全部测试离线，不调用真实 provider，不读取真实 `~/.looklift`。

## 人工验收（作者，后置）

- [ ] 启动进入首页；未来入口标注清楚且不会触发扫描或设备访问。
- [ ] 用原生文件选择器快速打开照片，并从首页继续同一正式会话。
- [ ] 同时打开两张照片，分别手调、对话和生成候选，确认切换后状态不串。
- [ ] AI 运行中切换标签，确认结果只回到原标签；非活动标签不接收拖放。
- [ ] 分别验证“保留并关闭 / 放弃并关闭 / 取消”和 AI 运行中关闭。
- [ ] 重启后只恢复正式版本；未确认预览不出现。
- [ ] 移动或删除源文件后确认首页标记不可用，不自动猜测新位置。
- [ ] 在约 1440px、1100px 和 900px 检查导航、标签、画布和面板。

## 完成条件

自动化与人工验收全部通过、实现文档已回填即完成；不顺带实现 v2.3 图库、设备导入或开始 v2.3。
