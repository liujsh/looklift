# RAW 可行性门实施计划

1. 先以离线替身测试固定 manifest、报告形状、失败隔离和决策规则。
2. 实现可选 rawpy 适配、RGB/位深/方向检查及安全测量边界。
3. 接入 `python -m looklift.raw_gate` 报告入口，补中文摘要和降级建议。
4. 用替身验证现有预览/渲染边界，回填架构与开发日志；真实相机样本留给 T5 作者验收。

## 完成条件

T1–T4 自动化与文档回填完成；T5 真实样本门禁由作者执行并确认后，才允许进入 v2.3-B。
